import {makeProject} from '@motion-canvas/core';

import example from './scenes/example?scene';
import animation from './scenes/animation?scene';

export default makeProject({
  scenes: [/*example, */animation],
});
